# Plan 11-03: CLI Diagnostics And Optional Setup Docs

## Goal

Expose user-facing setup diagnostics and document the optional diarization
configuration surface.

## Requirements

- DIAR-01
- DIAR-02
- DIAR-03

## Tasks

- Add `saymo diarization-check` with engine/model/device overrides.
- Print concise diagnostics for disabled, ready, missing dependency, and
  missing token states.
- Update public config/docs references with optional diarization examples.
- Add CLI coverage with `CliRunner`.

## Verification

- `.venv/bin/python -m pytest -q tests/test_config.py tests/analysis/test_diarization.py tests/test_trigger_check.py`
