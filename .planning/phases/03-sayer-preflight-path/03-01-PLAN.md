# Plan 03-01: Add Live-Call Preflight Command

## Requirements

- SAY-01: Verify cache, devices, provider readiness, triggers, and fallback
  status before a call.

## Tasks

- Add `saymo auto-preflight -p <profile>`.
- Check prepared standup cache, capture input, playback output, BlackHole 2ch
  provider output, provider tab readiness, triggers, response cache coverage,
  fallback mode, and live tuning.
- Print a final `preflight: ready/not ready` result.

