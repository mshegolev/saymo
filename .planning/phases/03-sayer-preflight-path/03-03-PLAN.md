# Plan 03-03: Harden Cache-Miss Fallback Behavior

## Requirements

- SAY-04: Auto-mode remains responsive when response-cache lookup misses.

## Tasks

- Add response route metadata for cache hits, classifier hits, live fallback,
  and standup fallback.
- Keep the existing prepared standup fallback as the default cache-miss path.
- Make live fallback failures return standup fallback metadata instead of
  breaking the trigger loop.

