# Plan 03-02: Normalize Forced-Playback Blocked Reasons

## Requirements

- SAY-03: Forced prepared playback returns a precise blocked reason when it
  cannot play.

## Tasks

- Return `PlaybackResult` from `_play_cached_audio()`.
- Emit structured blocked reasons for missing audio, missing BlackHole output,
  and missing provider tab.
- Preserve existing fallback behavior when provider mute automation fails.

