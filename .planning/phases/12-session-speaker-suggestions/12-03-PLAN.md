# Plan 12-03: Session Speaker Summary Command Output

## Goal

Expose speaker-cluster summaries and mapping commands so users can understand
and label diarization speaker ids before promotion.

## Requirements

- SPKR-01
- SPKR-02

## Tasks

- Add `trigger-sessions speakers` for cluster summaries and unresolved counts.
- Add `trigger-sessions map-speaker` for session-local speaker-id mappings.
- Recompute suggestion labels after mapping without touching sample `speaker`
  metadata.
- Add CLI tests for summary, mapping, and unchanged manual labels.

## Verification

- `.venv/bin/python -m pytest -q tests/analysis/test_diarization.py tests/test_trigger_check.py`
