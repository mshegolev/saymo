# Plan 10-02: Add Holdout Evaluation Reporting

## Requirements

- CLS-02: User can run a local holdout evaluation for the trained classifier and
  see precision/recall-style quality metrics for answer vs skip.

## Tasks

- Split labeled samples deterministically into train/holdout sets.
- Train on the train split and evaluate accepted/rejected predictions on the
  holdout split.
- Report confusion matrix, precision, recall, accuracy, and sample counts.
- Add tests for deterministic metrics.

## Verification

- CLI and analysis tests for `trigger-classifier evaluate`.

