# Plan 02-01: Add Profile-Level Catcher Tuning Config

## Requirements

- CATCH-02: Configure window, overlap, cooldown, and silence handling per
  meeting profile without source edits.

## Tasks

- Add global `live:` config defaults.
- Add optional `meetings.<profile>.live` overrides.
- Clamp invalid timing values to safe minimums.
- Document the config in `config.example.yaml` and quick-start docs.

