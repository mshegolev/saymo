# Plan 02-03: Add Promote-And-Rerun Workflow

## Requirements

- CATCH-04: Promote a heard trigger variant from an evaluated sample and re-run
  evaluation.

## Tasks

- Add `saymo trigger-eval --promote <sample.json>`.
- Extract the likely name variant using the existing trigger setup extractor.
- Write the variant through the existing fuzzy-expansion update path.
- Reload config and evaluate with the updated trigger variants.

