# Plan 09-01: Extend Sample Filtering And Disagreement Detection

## Requirements

- REV-01: User can filter saved samples by profile, session, category, speaker,
  answer decision, current classifier disagreement, and date range.

## Tasks

- Add reusable filter options for session id/prefix, speaker, answer decision,
  date range, and classifier disagreement.
- Extend record iteration/evaluation so list/report commands can reuse the same
  filtering semantics.
- Add CLI tests covering each filter and classifier disagreement filtering.

## Verification

- `pytest tests/test_trigger_check.py -q -k "trigger_samples"`

