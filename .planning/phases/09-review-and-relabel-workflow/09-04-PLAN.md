# Plan 09-04: Add Session-Aware Sanitized Reports And Docs

## Requirements

- REV-04: User can export a sanitized training-review report grouped by session
  and category, without raw audio payloads or private config values.

## Tasks

- Group `trigger-samples report` output by session id/name and category.
- Include aggregate counts for speakers and answer decisions.
- Keep transcript text out of the report.
- Update Quick Start and PRD command references.

## Verification

- Report test proving grouping and transcript omission.
- `saymo trigger-samples report --help`
