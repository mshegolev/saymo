# Plan 16-04: Wire Local Composer Drafting And Fallback Behavior

## Goal

Allow optional local Ollama composition while keeping deterministic fallback
drafts available for dry runs and tests.

## Requirements

- ANS-01
- ANS-02
- ANS-03

## Tasks

- Add optional `--compose` mode to `answer-draft`.
- Reuse `saymo.speech.ollama_composer.answer_question` for local LLM drafting.
- Fall back to deterministic cited draft on composer failure unless strict mode
  is requested.
- Cover default deterministic behavior with tests.

## Verification

- `.venv/bin/python -m pytest -q tests/analysis/test_answer_cockpit.py tests/test_meeting_memory_cli.py`
