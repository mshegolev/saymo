# Plan 14-03: Add Retention Config And Local Path Diagnostics

## Goal

Expose local transcript retention settings and show where ledgers are stored.

## Requirements

- MEM-02

## Tasks

- Add `meeting_memory` config with retention and default base-dir fields.
- Document the config keys in `config.example.yaml`.
- Add a CLI build command that prints retention and ledger path diagnostics.

## Verification

- `.venv/bin/python -m pytest -q tests/test_config.py tests/test_trigger_check.py`
