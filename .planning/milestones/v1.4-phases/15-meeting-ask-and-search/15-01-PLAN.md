# Plan 15-01: Add Local Meeting Search Filters And Result Rendering

## Goal

Search local meeting-memory ledgers by profile/session metadata and transcript
keywords.

## Requirements

- ASK-01

## Tasks

- Add search filter/result dataclasses.
- Load matching ledgers from local `_sessions` directories.
- Filter by profile, session id/prefix, date range, speaker, category, and
  keyword.
- Add `saymo meeting-search` command output and tests.

## Verification

- `.venv/bin/python -m pytest -q tests/analysis/test_meeting_memory.py tests/test_meeting_memory_cli.py`
