# Plan 10-01: Add Classifier Readiness Metrics

## Requirements

- CLS-01: User can run a readiness check that reports accepted/rejected label
  balance, category coverage, mention-vs-handoff coverage, and minimum sample
  thresholds per profile.

## Tasks

- Add readiness metric helpers over trigger sample records.
- Add `trigger-classifier readiness`.
- Report pass/fail plus missing coverage items.
- Add tests for ready and not-ready datasets.

## Verification

- `pytest tests/analysis/test_trigger_readiness.py tests/test_trigger_check.py -q -k readiness`
