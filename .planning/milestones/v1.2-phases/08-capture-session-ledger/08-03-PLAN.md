# Plan 08-03: Add Session List/Summary Commands And Docs

## Requirements

- SES-03: User can list prior capture sessions for a profile and inspect their
  time range, sample counts, and readiness status.

## Tasks

- Add `saymo trigger-sessions list`.
- Add `saymo trigger-sessions summary --session <id>`.
- Include session fields in `trigger-samples list` output when present.
- Document the session workflow in `docs/QUICK-START.md`.

## Verification

- CLI tests for `trigger-sessions list` and `trigger-sessions summary`.
- Documentation smoke check by running the new help commands.

