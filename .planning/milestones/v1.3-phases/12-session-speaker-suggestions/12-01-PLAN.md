# Plan 12-01: Load Session Audio Windows And Run Diarization Backend

## Goal

Add session-scoped diarization command flow that can either import deterministic
segments or invoke the configured optional backend.

## Requirements

- DIAR-04

## Tasks

- Resolve profile/session ids consistently with existing session summaries.
- Load session sample records and validate that the session has saved samples.
- Add deterministic `--segments-json` import for tests/manual backend output.
- Add optional pyannote runner behind Phase 11 availability checks.

## Verification

- `.venv/bin/python -m pytest -q tests/analysis/test_diarization.py tests/test_trigger_check.py`
