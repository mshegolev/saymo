# Plan 06-03: Add Shadow-Mode Classifier Diagnostics

## Requirements

- CLF-03: User can run the classifier in shadow mode during `trigger-eval` and
  `trigger-check`, comparing learned confidence against deterministic gating
  before enabling it.

## Tasks

- Add `--classifier-shadow` and `--model-dir` to `trigger-eval`.
- Add `--classifier-shadow` and `--model-dir` to `trigger-check`.
- Report learned accepted/rejected confidence and disagreement counts without
  changing deterministic answer decisions.
- Document the training and shadow-mode workflow.

## Verification

- CLI tests that prove `trigger-eval` and `trigger-check` print classifier
  diagnostics while preserving the normal deterministic output.
