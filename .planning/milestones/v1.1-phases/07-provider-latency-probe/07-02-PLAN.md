# Plan 07-02: Capture Segmented Provider/Playback Timings

## Requirements

- LAT-02: User can see end-to-end latency segments for capture, transcription,
  trigger decision, provider unmute, playback start, playback duration, and
  mute recovery.

## Tasks

- Time capture/transcription or mark them as `source=text` for deterministic
  dry runs.
- Time deterministic trigger/addressing classification.
- Time provider check, mic switch, unmute, playback start, playback duration,
  and mute recovery.
- Report blocked steps with a clear reason while preserving partial timing
  evidence.

## Verification

- CLI output assertions for every required segment.
- Blocked-path test for a missing active provider tab.
