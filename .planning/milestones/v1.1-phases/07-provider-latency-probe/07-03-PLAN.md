# Plan 07-03: Add Latency History Export and Docs

## Requirements

- LAT-03: User can export latency run history by profile and provider to
  identify recurring bottlenecks.

## Tasks

- Add serializable provider latency report objects.
- Write JSON and Markdown history under
  `~/.saymo/provider_latency/<profile>/<provider>/`.
- Document the command in README, Quick Start, and PRD command reference.
- Update roadmap, requirements, and project state.

## Verification

- Tests assert JSON and Markdown files are written by profile/provider.
- Full test suite and CLI help smoke checks.
