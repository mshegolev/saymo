# Plan 05-02: Make Trigger Evaluation Speaker-Aware

## Requirements

- SPK-02: User can run trigger evaluation grouped by speaker label and see
  answer decisions for `me`, `other`, and `unknown` speakers separately.

## Tasks

- Group evaluation rows by speaker.
- Print per-speaker record counts, misses, false positives, and answer counts.
- Keep existing aggregate evaluation output for backward compatibility.

## Verification

- CLI tests covering mixed `me`, `other`, and unlabeled samples.

